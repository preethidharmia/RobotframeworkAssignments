questions:
load a CSV file and display the first 10 rows along with column names.
Given a DataFrame, fill missing values using the mean of the respective column.
Write a program to group employees by department and calculate average salary.
Merge two DataFrames (employee details and salary detais) using merge().
Create a pivot table to show sales per month per region.
write a program to remove duplicate rows from a DataFrame.
Convert a column with categorical values into dummy variables using get_dummies().
Given a time-series DataFrame, resample it into weekly summaries.
Write a program to find the correlation matrix of all numerical columns.
Plot a bar chart showing total sales for each product category using Pandas built-in plotting.
