Validating multi user logins using credentials from CSV file and capturing screenshots.
User with issues (handled): user types below
  standard_user,
  locked_out_user,
  problem_user,
  performance_glitch_user.
